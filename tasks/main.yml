---

- validate:
    schema:
      type: object
      properties:
        command:
          type: string
          enum:
            - add_host_keys_to_known_hosts
            - harden
      required:
        - command
    instance: "{{ ssh }}"

- include_tasks: '{{ ssh.command }}.yml'
  when: ssh.command != 'dependency'